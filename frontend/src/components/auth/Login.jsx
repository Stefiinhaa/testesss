/**
 * React Login component (placeholder)
 *
 * Observação:
 * - O projeto atualmente usa páginas HTML estáticas para as telas de
 *   autenticação (login.html). Este arquivo existe para facilitar uma
 *   migração futura para SPA se desejado. Mantenha a compatibilidade com
 *   os ids `email` e `password` caso reimplemente a UI em React.
 */
